from django.apps import AppConfig

class NexotypeNeo4jConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.nexotype_neo4j'
    verbose_name = 'Nexotype - Neo4j Genomic Database'