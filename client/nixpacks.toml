[phases.setup]
nixPkgs = ['nodejs_20', 'npm-9_x']
aptPkgs = ['build-essential']

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
PORT = "3000"