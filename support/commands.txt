# Postgres
docker-compose -f support/docker-compose-postgres.yml up -d
docker-compose -f support/docker-compose-postgres.yml down
docker-compose -f support/docker-compose-postgres.yml logs
docker-compose -f support/docker-compose-postgres.yml down -v

docker stop n8n-ncw0cwgkgcs0gkcokcco8kk0
docker rm n8n-ncw0cwgkgcs0gkcokcco8kk0
docker ps | grep n8n

docker ps -a     # All containers (running + stopped)
docker ps        # Only running containers
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
docker volume ls
docker system df  # Show docker disk usage
docker builder prune -f # Clean up build cache
docker image prune -f # Clean up dangling images
docker image prune -a -f # Clean up unused images

find /data/coolify -name "docker-compose.yml" 2>/dev/null

# Stripe
stripe login
stripe listen --forward-to localhost:8000/api/accounts/subscriptions/webhook
stripe listen --forward-to localhost:8000/accounts/subscriptions/webhook
stripe trigger customer.subscription.updated
Your webhook signing secret is whsec_cb9d5ac886314d2bee6ebe4422830d3c9ac6fd0352ef389496d02dd5f2d26b5f

# Stripe — Nudgio (separate sandbox, must use --api-key to target the correct sandbox)
# Without --api-key, stripe listen connects to whichever sandbox was last used with stripe login.
# Each project has its own sandbox (nexotype=51ShB2t, finpy=51MEJyv, nudgio=51T7GFC).
stripe listen --api-key sk_test_51T7GFCPDB2NRsjlqeBKRL2GyAyFUDEn5701hQ3yJFyB1XobbUROgrrib4J8ttXt7SYqqmZh1tUI0uLE1bJGwux9L00BdEiOsVC --forward-to localhost:8002/accounts/subscriptions/webhook
# Nudgio webhook secret (generated by stripe listen --api-key): whsec_8049dafa89462200e17d67afcec88dcef8df6637f59d6335478dca55ee588268

# Alembic
alembic init migrations
alembic init -t async migrations   --> this is an important command.

from apps.your_new_app.models import *    --> Add your models here for autogenerate to work.    
alembic revision --autogenerate -m "Add your_model_name"
alembic upgrade head
alembic stamp head --purge

alembic current
alembic history

Shopify Admin API access token text field                                          │
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

uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Django
Development:
python manage.py runuvicorn
- Auto-reload, async support, development testing

Production:
python manage.py rungunicorn
- Port 9000, production-ready, better performance

python manage.py collectstatic
python manage.py startapp
python manage.py createsuperuser
python manage.py dbshell

# Django Testing
python manage.py test
python manage.py test apps.accounts.tests.test_stripe
python manage.py test apps.accounts.tests.test_auth
python manage.py test apps.assetmanager.tests.test_assetmanager --keepdb

# Playwright Next.js Testing
npx playwright test
npx playwright test auth.test.ts
npx playwright test auth.test.ts --ui

# n8n
docker compose -f docker-compose-n8n.yml up -d

tree . -I 'venv' > structure.txt

# uv
uv init --python 3.13
uv add aiohttp
uv add -r requirements.txt
uv pip freeze > requirements.txt
uv sync --python 3.13  (syncs the virtual environment to match uv.lock/pyproject exactly)
/Users/rbtrsv/Developer/main/nexotype/server/.venv/bin/python

# Claude code
claude mcp add playwright npx @playwright/mcp@latest
claude mcp add --transport http notion https://mcp.notion.com/mcp

# Ubuntu Server
ssh root@91.98.44.218
htop
apt list --upgradable
apt update
apt upgrade -y

# Coolify
ls -la /data/coolify/
For your FastAPI PostgreSQL connection:
ssh -L 5432:localhost:6030 root@91.98.44.218

CLI AI Tools
codex resume
claude --resume
gemini > chat > save or resume in gemini terminal app.

Shopify
shopify app generate extension                                                              │
shopify app dev
shopify app deploy
https://admin.shopify.com/store/nudgio-dev-store/apps/nudgio
https://dev.shopify.com/dashboard/
https://partners.shopify.com/4786160/
Shopify Partners → Apps → Nudgio → Test your app